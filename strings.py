string = "harry potter"

# `title` method change the first letter of every word to upper case.

print(string.title())       #the dot(.) method tell python to act upon the var named `string`

#There are many other methods that change the case of the string like `upper` and `lower`.

print(string.upper())           # `upper` chages every letter of the string to capital form.
print(string.lower())